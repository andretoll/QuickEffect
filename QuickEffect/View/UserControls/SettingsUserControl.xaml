<UserControl x:Class="QuickEffect.View.UserControls.SettingsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuickEffect.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel Margin="20">
        <!--- Settings Pages -->
        <TabControl Padding="0 20">

            <!--- Appearance Settings -->
            <TabItem Header="Appearance">
                <StackPanel Orientation="Vertical">

                    <!--- Theme -->
                    <Label Content="Theme"/>
                    <ComboBox ItemsSource="{Binding Path=Themes}"
                              MaxDropDownHeight="200"                              
                              SelectedItem="{Binding SelectedTheme, Mode=TwoWay}" />

                    <!--- Accent -->
                    <Label Content="Accent"/>
                    <ComboBox ItemsSource="{Binding Path=Accents}"
                              MaxDropDownHeight="200"
                              SelectedItem="{Binding SelectedAccent, Mode=TwoWay}" />

                </StackPanel>
            </TabItem>

        </TabControl>
    </DockPanel>
</UserControl>
